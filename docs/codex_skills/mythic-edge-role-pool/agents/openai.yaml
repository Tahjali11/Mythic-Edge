interface:
  display_name: "Mythic Edge Role Pool"
  short_description: "Named inspect, gated dispatch, and Stage-4 canary"
  default_prompt: "Use $mythic-edge-role-pool to inspect and recommend the next compatible Codex B wave without mutation."
policy:
  allow_implicit_invocation: false
